{
  "name": "nutrition-app-2-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx playwright install chromium --with-deps && cd frontend && npm ci && npm run build",
    "start": "node server.js",
    "dev:server": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.54.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "compression": "^1.8.1",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "express-session": "^1.19.0",
    "pg": "^8.21.0",
    "playwright": "^1.60.0"
  }
}
